// Panel states
is_open = false;
target_x = 0;
target_y = 0;

// Panel dimensions
panel_w = 293;
panel_h = 360;

// Closed position (bottom left, mostly hidden)
closed_x = 20;
closed_y = 720 - 90; // only top 60px visible

// Open position (up and slightly right)
open_x = 70;
open_y = 720 - 360 - 50; // show full panel, leave margin at bottom

// Start position
x = closed_x;
y = closed_y;

